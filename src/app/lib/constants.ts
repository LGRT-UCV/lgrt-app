export enum Routes {
  Home = "/",
  Inventory = "/inventory",
  Projects = "/projects",
  Requests = "/requests",
  Profile = "/profile",
};

export enum Roles {
  External = "external",
  Internal = "internal",
  Admin = "admin",
};